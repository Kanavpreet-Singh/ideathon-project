<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Signup Options</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background: linear-gradient(135deg, #74ebd5, #9face6);
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        .container {
            text-align: center;
            background: white;
            padding: 2rem;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
        }

        h1 {
            font-size: 1.8rem;
            color: #333;
            margin-bottom: 1.5rem;
        }

        form {
            margin: 1rem 0;
        }

        button {
            font-size: 1rem;
            color: white;
            background-color: #28a745;
            border: none;
            padding: 0.8rem 1.5rem;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        button:hover {
            background-color: #218838;
        }

        button:focus {
            outline: none;
        }
    </style>
</head>
<body>

    <div class="container">
        <h1>Signup as</h1>
        <form action="/user/signup">
            <button type="submit">Citizen</button>
        </form>
        <form action="/user/signupOfficial">
            <button type="submit">Government Official</button>
        </form>
    </div>
    
</body>
</html>
